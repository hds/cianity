workflow ci {
    stage build {
        template base [
            step setup { rustup update }
            step build { cargo build }
        ]

        template extended (inherit = base) [
            steps,
            step test { cargo test }
        ]

        job compile (inherit = extended) [
            steps,
        ]
    }
}
