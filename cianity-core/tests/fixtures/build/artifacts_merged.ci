workflow ci {
    stage build {
        template base [
            step build { cargo build --release }
        ] -> [dist/]
        job compile (inherit = base) [
            steps,
        ] -> [target/release/app, **/*.so]
    }
}
