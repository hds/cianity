workflow ci {
    use {
        workflow (
            location = ../shared/templates.ci,
            name = shared,
        )
    }
    
    stage test {
        job unit ( inherit = shared/not_there ) [
            steps,
        ]
    }
}
