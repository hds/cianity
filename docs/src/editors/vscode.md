# VSCode

There is an extension for VS Code to add support for the Ciane DSL.

To build and install locally run the following from the `vscode-ciane` directory in the cianity repository.

```sh
npm install
npm run package
```

Then in VSCode: Extensions > "Install from VSIX".
