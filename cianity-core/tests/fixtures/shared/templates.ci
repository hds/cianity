workflow shared {
    template base [
        step setup { rustup update }
        step run { cargo test }
    ]
    
    stage extra {
        template integration [
            step setup { rustup update }
            step run { cargo test --test '*' }
        ]
    }
}
