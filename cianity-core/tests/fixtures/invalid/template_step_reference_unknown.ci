workflow ci {
    stage build {
        template base [
            step setup { rustup update }
        ]

        template quick ( inherit = base ) [
            step setpu,
        ]

        job fast ( inherit = quick ) [
            steps,
        ]
    }
}
