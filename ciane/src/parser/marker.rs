// This module is intentionally empty — the parser drives rowan's
// `GreenNodeBuilder` directly rather than through an intermediate marker layer.
// It is kept as a placeholder for future extension.
