# VSCode Extension for Ciane

This is a basic extension to add support for the Ciane language to VS Code.

To build and install locally:

```sh
npm install
npm run package
```

Then in VSCode: Extensions > "Install from VSIX".
