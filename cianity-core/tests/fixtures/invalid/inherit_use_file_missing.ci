workflow ci {
    use {
        workflow (
            location = ../shared/nonexistent.ci,
            name = shared,
        )
    }
    
    stage test {
        job unit ( inherit = shared/base ) [
            steps,
        ]
    }
}
