workflow ci {
    use shared ( path = ../shared/templates.ci )

    template local ( inherit = shared/base ) [
        steps,
        step extra { echo extra }
    ]

    stage test {
        job unit ( inherit = local ) [
            steps,
        ]
    }
}
